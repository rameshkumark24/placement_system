spring:
  datasource:
    url: jdbc:h2:mem:placement-system-test;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

jwt:
  secret: placementSystemJwtSecretKey2026SecureKey123!
  expiration-ms: 1800000
  refresh-expiration-ms: 604800000

app:
  legacy-migration:
    enabled: false
  cors:
    allowed-origins: http://localhost:5173,http://127.0.0.1:5173,http://localhost,http://127.0.0.1
