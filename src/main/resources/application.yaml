spring:
  application:
    name: placement-system
  datasource:
    url: jdbc:mysql://localhost:3306/placement_db
    username: root
    password: Sivakannan@52
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: placementSystemJwtSecretKey2026SecureKey123!
  expiration-ms: 1800000
